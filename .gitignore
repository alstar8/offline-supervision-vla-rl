__pycache__/
*.pyc
*.egg-info/
*.DS_Store
.idea/
.vscode/

paper/

# Local credentials.
.env
.env.*
!.env.example

/datasets/
/ManiSkill/mp_collect/
/openvla/checkpoints/
/openvla/wandb/
/SimplerEnv/wandb/
/SimplerEnv/wandb_backup/
/SimplerEnv/scripts/stats/

*.log
**/nohup*
wandb/
temp/
exp_local/

openvla/models

# Checkpoints and other large binaries; GitHub rejects files over 100 MB.
*.safetensors
*.ckpt
*.pt
*.pth
sim2real/assets/360_photos/

# TFDS builds.
**/datasets_v*/

# Run trees are output: collected episodes, eval videos, per-shard stats, CSV
# summaries, pids and lock files. Only the launchers and the analysis scripts
# that produce them are source, so re-include those and nothing else.
sim2real/runs/**
!sim2real/runs/**/
!sim2real/runs/**/*.sh
!sim2real/runs/**/*.py
